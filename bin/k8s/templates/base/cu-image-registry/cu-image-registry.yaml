{{- if .Values.cuImageRegistry.enabled }}
# Licensed to the Apache Software Foundation (ASF) under one
# or more contributor license agreements.  See the NOTICE file
# distributed with this work for additional information
# regarding copyright ownership.  The ASF licenses this file
# to you under the Apache License, Version 2.0 (the
# "License"); you may not use this file except in compliance
# with the License.  You may obtain a copy of the License at
#
#   http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
#
# Registry holding the mirrored copies of curated computing-unit images.
#
# Two different clients have to agree on one address for an image, and they
# resolve names differently: the skopeo job pushing the copy is a pod and uses
# cluster DNS, while the kubelet pulling it is not and does not. A ClusterIP is
# the one address both can reach, which is why this Service pins one instead of
# letting Kubernetes assign it -- an image reference embeds the address, so it
# has to survive a reinstall of the chart.
#
# The registry is plain HTTP. That is only workable because container runtimes
# are already configured to treat the Service CIDR as insecure (minikube trusts
# 10.96.0.0/12 out of the box). A real deployment terminates TLS here and gives
# the pull a credential.
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.cuImageRegistry.name }}
  namespace: {{ .Release.Namespace }}
spec:
  type: ClusterIP
  clusterIP: {{ .Values.cuImageRegistry.clusterIP }}
  selector:
    app: {{ .Values.cuImageRegistry.name }}
  ports:
    - name: registry
      port: {{ .Values.cuImageRegistry.port }}
      targetPort: 5000
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.cuImageRegistry.name }}
  namespace: {{ .Release.Namespace }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ .Values.cuImageRegistry.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.cuImageRegistry.name }}
    spec:
      containers:
        - name: registry
          image: {{ .Values.cuImageRegistry.image }}
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 5000
          env:
            # Needed for the registry's own garbage collection to be able to reclaim
            # layers, which is what eventually frees the tags a re-mirror leaves behind.
            - name: REGISTRY_STORAGE_DELETE_ENABLED
              value: "true"
          volumeMounts:
            - name: images
              mountPath: /var/lib/registry
          readinessProbe:
            httpGet:
              path: /v2/
              port: 5000
            initialDelaySeconds: 3
            periodSeconds: 5
      volumes:
        - name: images
          {{- if .Values.cuImageRegistry.persistence.enabled }}
          persistentVolumeClaim:
            claimName: {{ .Values.cuImageRegistry.name }}-pvc
          {{- else }}
          emptyDir: {}
          {{- end }}
{{- if .Values.cuImageRegistry.persistence.enabled }}
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.cuImageRegistry.name }}-pvc
  namespace: {{ .Release.Namespace }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.cuImageRegistry.persistence.size }}
{{- end }}
{{- end }}
